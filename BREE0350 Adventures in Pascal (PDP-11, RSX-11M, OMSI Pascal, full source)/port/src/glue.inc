{ glue.inc - declarations the separately compiled OMSI modules each made for
  themselves and that one Free Pascal program needs exactly once. }
TYPE ARR5=ARRAY[1..5] OF INTEGER;	{ ASK.PAS, WIZMAG.PAS }
     OMSIDBL=ARRAY[1..2] OF INTEGER;	{ the hours masks: ADVGBL, HOURS, PRIMET, START }
